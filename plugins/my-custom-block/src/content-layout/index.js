/**
 * Not a real block — see block.json. This entry exists solely so the build
 * pipeline compiles the Post / Customer Case content-width stylesheet,
 * which my-custom-block.php enqueues directly (by file path) on both the
 * frontend and the block editor, only for those two post types.
 */
import './style.css';
