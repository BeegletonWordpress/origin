/**
 * Not a real block — see block.json. This entry exists solely so the build
 * pipeline compiles the site-wide stylesheet that used to live in the Site
 * Editor's "Additional CSS", which my-custom-block.php prints inline (by
 * file path) in the same cascade position that CSS had.
 */
import './style.css';
