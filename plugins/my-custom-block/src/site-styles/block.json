{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "create-block/site-styles",
	"version": "0.1.0",
	"title": "Site Styles (internal — not a real block)",
	"category": "theme",
	"description": "Not a registered block. Exists only so the build pipeline compiles src/site-styles/style.css (the site's former Global Styles 'Additional CSS'), which my-custom-block.php prints inside the global-styles inline style on the frontend and next to the global-styles CSS in the block editor.",
	"textdomain": "my-custom-block",
	"editorScript": "file:./index.js"
}
